---
/**
 * * Contact section for the website
 */

import { Image } from "astro:assets";

// components
import ContactForm from "@components/Forms/ContactForm.astro";

interface Props {
  class?: string;
}

const { class: className } = Astro.props as Props;

import ringsImage from "@assets/images/rings-min.jpg";
---

<section
  id="contact"
  class="mx-auto max-w-md scroll-mt-10 px-4 py-8 md:max-w-6xl md:py-12"
  class:list={[className]}
>
  <h2 class="h2 md:px-12 lg:text-6xl">Lets Talk!</h2>
  <div class="grid gap-y-10 md:grid-cols-2">
    <div class="border-primary-300 pt-8 md:border-r md:pl-12 md:pr-20">
      <p class="description text-lg">
        Have questions or are you ready to get started? 
        I’m here to help! Fill out the form, and I’ll get back to you within 1-2 business days. 
        Let’s work together to bring your vision to life.
      </p>
      <div class="mx-auto mt-8 aspect-square overflow-hidden">
        <Image
          src={ringsImage}
          alt="Lets Talk"
          width={450}
          densities={[1.5, 2]}
          class="h-full w-full object-cover"
        />
      </div>
    </div>
    <div class="md:pl-20 md:pr-12">
      <ContactForm />
    </div>
  </div>
</section>
